# 机构分组（1.6.1-gold.3）

账户列表 → 机构分组：银行与支付平台。桌面和手机共用一套实现。

- 一层可选机构归属，可跨账户分类；创建、改名、移动、移出、删除分组。
- 仅根账户指定归属，已有父子账户一起移动。不会自动创建用户账户。
- 分组不持有余额，不生成交易，不修改账户分类、黄金克数或成本。黄金仍由原有黄金资产模块维护。
- 汇总按币种分别显示资产、负债、净额，无自动汇率转换。父账户不与子账户重复计数。
- 默认不显示隐藏账户；本页开关可包含隐藏账户。遵从当前客户端“不计入总金额”设置，该设置不同客户端可能不同。
- 黄金计入账面成本，并非市值；净额不是可投资资金。信用卡溢缴按现有账本负债符号规则显示。
- 删除分组只删除归属，账户回到未分组。网络失败隐藏未确认快照，刷新核对后重试。

## 数据与兼容

仅增加 account_group 与 account_group_member 两张元数据表，不改现有账户或交易表结构/契约。启动同步迁移；旧版本忽略两表。按用户隔离，根账户至多一组；服务端用户锁串行化修改，分组 version 与 expectedGroupId 拒绝过期覆盖。

认证接口：GET /api/v1/account-groups/list.json；POST save.json、delete.json、assign.json（同路径前缀）。普通 API Token 仍可能可写，不是只读权限。

分组在完整 PostgreSQL 备份中保存。上游 CSV/JSON 便携导入导出尚不包含分组，不应把这些导出当成完整备份。回滚到 gold.2 会隐藏分组功能，保留新增表；不删除两表、不覆盖现有账本。数据库回滚必须配合一致性备份与附件，不能只承诺替换镜像。

研究、采集、AI、Cloudflare 规则和定时任务不因分组改变。没有将真实财务数据自动接入研究报告。

## 验证

覆盖多币种、负债、隐藏和排除设置、父子去重、金额精度；后端覆盖归属、跨用户隔离、版本冲突、并发移动、黄金成本账户兼容与账户原行不变。另行运行 NAS PostgreSQL 副本升级、HTTP CRUD、容器重建及恢复演练，实际结果见部署工程验收记录。
