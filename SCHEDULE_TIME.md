# 定时交易执行时间（gold.5）

桌面和手机的原定时交易表单增加执行时间（HH:mm，按交易时区）。未设置的新任务默认00:00；旧任务不迁移，仍按原零点执行。时间与原开始/结束日期共同生效。

复用 scheduled_at（UTC 日内分钟）列，不增加数据表。新增可选 scheduledMinute 请求字段为本地日内分钟0–1439；创建省略默认零点，编辑省略保留原本地执行时间，避免旧客户端编辑时重置自定义时间。响应继续使用原 scheduledAt 和 utcOffset。时区继续沿用原固定UTC偏移契约，不承诺夏令时自动调整。

仅 CreateScheduledTransaction 调度改为每分钟秒0检查；其他应用后台任务不变。NAS服务自行执行，不依赖Mac。停机错过的分钟沿用原行为，不自动补记；沿用原单实例执行和失败处理，不承诺 exactly-once、自动核实银行扣款或重试不重复。

验证：前端类型检查、时间序列化/旧值/时区测试；后端请求范围校验、分钟边界调度、隔离数据库转账守恒、开始前和结束后不执行。真实银行转账由用户自己的银行/支付宝设置执行，本功能只生成记账记录。
